apiVersion: apps/v1
kind: Deployment
metadata:
  name: eureka-provider
  labels:
    app: eureka-provider
spec:
  replicas: 2
  selector:
    matchLabels:
      app: eureka-provider
  template:
    metadata:
      labels:
        app: eureka-provider
    spec:
      containers:
      - name: eureka-provider
        image: ccr.ccs.tencentyun.com/tsf_100020616957/eureka-provider:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8081
        command:
          - /bin/bash
          - -c
          - cd /app && java -Deureka.client.serviceUrl.defaultZone=http://192.168.109.30:8761/eureka/ -Deureka.instance.preferIpAddress=true -jar main.jar
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8081
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8081
          initialDelaySeconds: 30
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: eureka-provider
  labels:
    app: eureka-provider
spec:
  selector:
    app: eureka-provider
  ports:
  - protocol: TCP
    port: 8081
    targetPort: 8081
    name: http
  type: ClusterIP
